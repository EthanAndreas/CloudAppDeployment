---
- hosts: all
  handlers:
    - name: Reload systemd
      ansible.builtin.systemd:
        daemon_reload: true
  roles:
    - role: consul
      tags: consul
    - role: docker
      tags: docker
    - role: nomad
      tags: nomad
    - role: keepalived
      tags: keepalived 